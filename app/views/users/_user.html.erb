<li>
　<%= gravatar_for(user) %> <%= link_to user.name, user %>
	<% if current_user.admin? && !current_user?(user)  %>
		<!-- !current_user?(user) が等しくないですよね？ trueの場合 -->
		<%= link_to("Delete", user_path(user.id), method: :delete, data: {confirm: "Are you Sure?"}) %>
	<% end %>
</li>